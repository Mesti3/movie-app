name: Build and deploy

on:
  push:
    branches: [master]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Prepare
        run: |
          npm install -g @angular/cli
          npm install
          npm run build
          cd dist
          npm link
          cd ..
          cd ..
          ng new movie-app-gh --defaults
          cd movie-app-gh
          npm link angular-cli-ghpages
          ng add angular-cli-ghpages

      - name: Deploy
        if: github.ref == 'refs/heads/main'
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}
        run: |
          cd movie-app-gh
          ng deploy --name="The Movie app" --email="mesticky.jakub7@gmail.com" --cname=angular-cli-ghpages.angular.schule
